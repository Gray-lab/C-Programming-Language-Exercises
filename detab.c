#include <stdio.h>

#define MAXLINE 1000

int main()
{
    // read in a line

    // while reading, keep track of count of printing characters (col) and replace tabs with spaces

    // when a tab is encountered, if 
    // col % 4 == 0, add 4 spaces
    // col % 4 == 1, add 3 spaces
    // col % 4 == 2, add 2 spaces
    // col % 4 == 3, add 1 space

    // when a tab is encountered replace with (4 - col % 4) spaces
}

void get_line(int lim)
{
}